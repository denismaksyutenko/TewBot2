package without.TewBot.service;

/*import com.google.api.GoogleAPI;
import com.google.api.translate.Language;
import com.google.api.translate.Translate;

public class TranslationService {
        public static void main(String[] args) {
            // Встановлення мови перекладу
            String sourceLanguage = "en";
            String targetLanguage = "uk";

            // Вхідний текст для перекладу
            String inputText = "Hello, world!";

            // Ініціалізація Google API
            GoogleAPI.setHttpReferrer("your-referrer-key");
            GoogleAPI.setKey("your-google-api-key");

            try {
                // Переклад тексту
                String translatedText = Translate.DEFAULT.execute(inputText, Language.fromString(sourceLanguage),
                        Language.fromString(targetLanguage));

                // Виведення перекладеного тексту
                System.out.println("Translated Text: " + translatedText);
            } catch (Exception e) {
                e.printStackTrace();
            }
        }
    }*/


